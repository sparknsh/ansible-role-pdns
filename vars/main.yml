---

pdns_auth_powerdns_repo_master:
  apt_repo_origin: "repo.powerdns.com"
  apt_repo: "deb [arch=amd64] {{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/{{ ansible_distribution | lower }} {{ ansible_distribution_release | lower }}-auth-master main"
  gpg_key: "{{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/CBC8B383-pub.asc"
  gpg_key_id: "D47975F8DAE32700A563E64FFF389421CBC8B383"
  yum_repo_baseurl: "{{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/centos/$basearch/$releasever/auth-master"
  yum_debug_symbols_repo_baseurl: "{{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/centos/$basearch/$releasever/auth-master/debug"
  name: "powerdns-auth-master"

pdns_auth_powerdns_repo_40:
  apt_repo_origin: "repo.powerdns.com"
  apt_repo: "deb [arch=amd64] {{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/{{ ansible_distribution | lower }} {{ ansible_distribution_release | lower }}-auth-40 main"
  gpg_key: "{{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/FD380FBB-pub.asc"
  gpg_key_id: "9FAAA5577E8FCF62093D036C1B0C6205FD380FBB"
  yum_repo_baseurl: "{{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/centos/$basearch/$releasever/auth-40"
  yum_debug_symbols_repo_baseurl: "{{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/centos/$basearch/$releasever/auth-40/debug"
  name: "powerdns-auth-40"

pdns_auth_powerdns_repo_41:
  apt_repo_origin: "repo.powerdns.com"
  apt_repo: "deb [arch=amd64] {{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/{{ ansible_distribution | lower }} {{ ansible_distribution_release | lower }}-auth-41 main"
  gpg_key: "{{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/FD380FBB-pub.asc"
  gpg_key_id: "9FAAA5577E8FCF62093D036C1B0C6205FD380FBB"
  yum_repo_baseurl: "{{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/centos/$basearch/$releasever/auth-41"
  yum_debug_symbols_repo_baseurl: "{{ 'http://' if not pdns_repo_https else 'https://' }}repo.powerdns.com/centos/$basearch/$releasever/auth-41/debug"
  name: "powerdns-auth-41"

pdns_service_name: "pdns"

pdns_config_file: "pdns.local.conf"

pdns_user: pdns
pdns_group: pdns

